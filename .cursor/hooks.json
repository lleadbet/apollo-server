{
  "version": 1,
  "hooks": {
    "beforeShellExecution": [
      {
        "command": "bash .claude/hooks/changeset-reminder.sh --format cursor",
        "matcher": "git commit"
      }
    ]
  }
}
